services:
  - type: web
    name: code-comment
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ai_code_comment:app --bind 0.0.0.0:$PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
